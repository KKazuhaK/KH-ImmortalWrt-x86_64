# ImmortalWrt x86 (32-bit) — minimal target-only config.
# Suitable for ancient PCs / Pentium / very low-end VMs (i686).
# Almost no one needs this in 2026; included mostly for completeness.

CONFIG_TARGET_x86=y
CONFIG_TARGET_x86_generic=y
CONFIG_CCACHE=y
